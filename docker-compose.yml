version: '3.8'

services:
  db:
    image: postgres:15-alpine
    restart: always
    command: postgres -c "log_statement=all"
    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
      POSTGRES_DB: flask_db
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - app-network

  redis:
    image: redis:7-alpine
    restart: always
    networks:
      - app-network

  web:
    build: ./app
    restart: always
    command: sh -c "yoyo apply --batch && gunicorn -w 4 -b 0.0.0.0:8000 app:app"
    environment:
      DATABASE_URL: postgresql://user:password@db:5432/flask_db
      REDIS_URL: redis://redis:6379/0
    depends_on:
      - db
      - redis
    networks:
      - app-network

  nginx:
    image: bitnami/nginx:latest
    restart: always
    ports:
      - "80:8080"
    volumes:
      - ./nginx/nginx.conf:/opt/bitnami/nginx/conf/server_blocks/my_vts.conf:ro
      - nginx_logs:/var/log/nginx
    depends_on:
      - web
    networks:
      - app-network
  
  prometheus:
    image: prom/prometheus:latest
    container_name: prometheus
    volumes:
      - ./prometheus/prometheus.yml:/etc/prometheus/prometheus.yml
      - prometheus_data:/prometheus
    command:
      - '--config.file=/etc/prometheus/prometheus.yml'
    ports:
      - "9090:9090"
    networks:
      - app-network

  grafana:
    image: grafana/grafana:latest
    container_name: grafana
    environment:
      - GF_SECURITY_ADMIN_PASSWORD=admin
    volumes:
      - grafana_data:/var/lib/grafana
    ports:
      - "3000:3000"
    networks:
      - app-network

  node-exporter:
    image: prom/node-exporter:latest
    container_name: node-exporter
    restart: unless-stopped
    volumes:
      - /proc:/host/proc:ro
      - /sys:/host/sys:ro
      - /:/rootfs:ro
    command:
      - '--path.procfs=/host/proc'
      - '--path.rootfs=/rootfs'
      - '--path.sysfs=/host/sys'
      - '--collector.filesystem.mount-points-exclude=^/(sys|proc|dev|host|etc)($$|/)'
    networks:
      - app-network

  nginx-vts-exporter:
    image: sophos/nginx-vts-exporter
    environment:
      - NGINX_STATUS=http://nginx:8080/status/format/json
    ports:
      - "9913:9913"
    networks:
      - app-network
    depends_on:
      - nginx

  postgres-exporter:
    image: prometheuscommunity/postgres-exporter
    container_name: postgres-exporter
    environment:
      DATA_SOURCE_NAME: postgresql://user:password@db:5432/flask_db?sslmode=disable
    networks:
      - app-network
    depends_on:
      - db


volumes:
  nginx_logs:
  postgres_data:
  prometheus_data:
  grafana_data:

networks:
  app-network:
